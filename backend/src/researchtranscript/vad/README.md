Silero VAD v5.1.2 als ggml (`silero-v5.1.2.bin`, 885 KB) — die
Sprachaktivitätserkennung von whisper.cpp (`--vad`). Quelle:
https://huggingface.co/ggml-org/whisper-vad (Konvertierung von
ggml-org, Modell von Silero, beide MIT). Liegt im Python-Paket, damit
das Bundle sie sicher hat; heisst bewusst nicht `ggml-*.bin`, damit der
Modell-Scan der Einstellungen sie nicht als Whisper-Modell anbietet.
