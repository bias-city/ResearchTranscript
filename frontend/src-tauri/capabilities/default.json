{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Hauptfenster: Dialoge (Öffnen/Speichern), Drag-Drop, Events. Kein Web-Inspector: die Oberfläche ruft ihn nicht auf, und ein eingereichtes Paket soll kein Entwicklerwerkzeug freigeben.",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "core:window:allow-set-focus",
    "core:event:default",
    "dialog:allow-save",
    "dialog:allow-open"
  ]
}
