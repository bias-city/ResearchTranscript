<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Developer-ID-Kanal (DMG): Hardened Runtime OHNE Ausnahmen.

  Bis 0.4.0 brauchte die Hülle vier cs.*-Ausnahmen (Library-Validation
  aus, JIT, unsigned-executable-memory, dyld-Variablen), weil ein
  fremd signiertes venv als Kindprozess lief. Seit Python im Prozess
  läuft (Plan §6): jede .so/.dylib trägt die eigene Team-Signatur
  (scripts/sign-resources.mjs), CPython 3.13 braucht kein JIT, und es
  gibt keine PYTHON*/DYLD_-Variablen mehr. Im Phase-0-Spike ohne diese
  Schlüssel verifiziert.

  Der Store-Kanal bekommt eine eigene Datei (entitlements.mas.plist:
  app-sandbox, files.user-selected, bookmarks, network.client).
-->
<plist version="1.0">
<dict>
</dict>
</plist>
